import { createStyles } from "@material-ui/core/styles";

export default createStyles({
  cardMedia: {
    objectFit: "fill",
  },
  modal: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
});
