import { createStyles } from "@material-ui/core";

export default createStyles({
  grid: {
    overflow: "scroll",
    width: "auto", //override material-ui grid's width
    margin: 0, //override material-ui gride's margin
  },
});
